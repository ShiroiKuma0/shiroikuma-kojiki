<?xml version="1.0" encoding="utf-8"?>
<!-- Fork (白い熊 考直): 白い熊 考直 UI settings page. Rows are built programmatically into the
     holder by KojikiUiActivity (sections + indented colour/font controls). -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="?attr/background">

    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/kojiki_ui_toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        app:title="@string/kojiki_ui_title" />

    <androidx.core.widget.NestedScrollView
        android:id="@+id/kojiki_ui_scroll"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:fillViewport="true">

        <LinearLayout
            android:id="@+id/kojiki_ui_holder"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingTop="8dp"
            android:paddingBottom="24dp" />

    </androidx.core.widget.NestedScrollView>

</LinearLayout>
